{
  "services": {
    "api": {
      "root": ".",
      "framework": "express",
      "installCommand": "npm install",
      "buildCommand": "npx prisma generate"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": { "service": "api" } }
  ]
}
